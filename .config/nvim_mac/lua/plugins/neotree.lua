return {
  "nvim-neo-tree/neo-tree.nvim",

  opts = {
    close_if_last_window = true,
    autochdir = true,
    follow_current_file = {
      enabled = true,
    },
    group_empty_dirs = true,
    window = {
      position = "right",
      width = 25,
    },
  },
}
